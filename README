This is a template for Arduino OTA-implementation. Needs a switch to exit sleep-mode routine for it to work. This is not implemented in code yet. 


Wokring zip-command for ChatGPT
zip -r Tamper_temp.zip . -x "*.git*" "*.pio*" "*.DS_Store"

